apiVersion: v1
kind: Service
metadata:
  name: product-frontend
  labels:
    app: product-frontend
spec:
  type: NodePort
  selector:
    app: product-frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080
